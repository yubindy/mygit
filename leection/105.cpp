#include <bits/stdc++.h>
class Solution {
private:
    
public:
    TreeNode *buildTree(vector<int> &preorder, vector<int> &inorder) {
        if (preorder.size() <= 1) {
            return preorder;
        }
    }
};